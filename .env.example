# Copy to .env and fill in. .env is gitignored; this file is not.

# Only needed for the Groq backend (Ctrl+Alt+G). The local backend needs nothing.
# Get a key at https://console.groq.com
GROQ_API_KEY=

# Optional: point the HuggingFace cache somewhere with room for the ~3 GB model.
# HF_HOME=
