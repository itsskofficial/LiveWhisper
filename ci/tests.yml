name: tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: ${{ matrix.os }} · python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # The app is Windows-only - screen reading, hotkeys and loopback capture
        # are Windows APIs - and several suites import those modules.
        os: [windows-latest]
        python: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip

      - name: Install dependencies (CPU only)
        shell: bash
        run: |
          python -m pip install --upgrade pip
          # No GPU here, so skip the ~1 GB of CUDA runtime wheels and take the
          # CPU build of torch, which is all the 4.5M-parameter spelling model
          # needs. No model weights are downloaded: speech-model suites use
          # stand-ins, and the real-speech suites are behind --audio.
          grep -v '^nvidia-' requirements.txt > requirements-ci.txt
          pip install -r requirements-ci.txt
          pip install torch --index-url https://download.pytorch.org/whl/cpu

      - name: Test suites
        shell: bash
        env:
          PYTHONIOENCODING: utf-8
        run: python run_tests.py

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install ruff
      - name: ruff
        run: ruff check livewhisper tools --output-format=github
