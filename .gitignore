.env
.venv/
__pycache__/
*.pyc
transcripts/
*.wav

*.log
shot*.png
accuracy_runs/

*.pt
!data/translit.pt

build/

# what the app learns and writes about its user - never commit it
profiles.json
notes/
openrouter_usage.json
models/

# the app's downloaded language models, in a source checkout
/llm/
/models/

# runtime files of a source checkout, and fetched build inputs
/history.jsonl
/instance.port
/vendor/
/webview/
/cuda/

# stray lock files from GPU benchmark runs
tests/.gpu-load.lock
